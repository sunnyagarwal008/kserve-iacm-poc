pipeline:
  name: kserve-m0-apply
  identifier: kservem0apply
  projectIdentifier: sunnyplayground
  orgIdentifier: default
  tags:
    poc: kserve-iacm
    milestone: m0
  description: Init/plan/apply on GKE iac-team-standard via iacteamstandard. Set offline_plan=false.
  variables:
    - name: runner_namespace
      type: String
      description: Namespace whose default service account runs the IaCM step pods.
      value: <+input>.default(kserve-m0)
  stages:
    - stage:
        name: apply-on-cluster
        identifier: applyoncluster
        description: Run OpenTofu in-cluster so InheritFromDelegate kube credentials work.
        type: IACM
        spec:
          workspace: <+input>
          infrastructure:
            type: KubernetesDirect
            spec:
              connectorRef: iacteamstandard
              namespace: <+pipeline.variables.runner_namespace>
              automountServiceAccountToken: true
              os: Linux
          execution:
            steps:
              - step:
                  type: IACMOpenTofuPlugin
                  name: init
                  identifier: init
                  timeout: 10m
                  spec:
                    command: init
              - step:
                  type: IACMOpenTofuPlugin
                  name: plan
                  identifier: plan
                  timeout: 10m
                  spec:
                    command: plan
              - step:
                  type: IACMOpenTofuPlugin
                  name: apply
                  identifier: apply
                  timeout: 15m
                  spec:
                    command: apply
        tags: {}
